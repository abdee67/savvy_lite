import 'package:flutter/material.dart';

class InventoryTransactionReport extends StatelessWidget {
  const InventoryTransactionReport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Inventory Transaction Report')),
      body: const Center(child: Text('Under Construction')),
    );
  }
}
