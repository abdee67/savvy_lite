import 'package:flutter/material.dart';

class ItemCostReport extends StatelessWidget {
  const ItemCostReport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Item Cost Report')),
      body: const Center(child: Text('Under Construction')),
    );
  }
}
