import 'package:flutter/material.dart';

class InventoryMovementReport extends StatelessWidget {
  const InventoryMovementReport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Inventory Movement Report')),
      body: const Center(child: Text('Under Construction')),
    );
  }
}
