import 'package:flutter/material.dart';

class UpcomingExpirationReport extends StatelessWidget {
  const UpcomingExpirationReport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Upcoming Expiration Report')),
      body: const Center(child: Text('Under Construction')),
    );
  }
}
